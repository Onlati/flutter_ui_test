name: flutter_ui_test
description: "CKS Test"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  share_plus: ^10.1.4

  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.7.0
  keyboard_actions: ^4.2.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/fondo.png
    - assets/images/fondo-claro.png
